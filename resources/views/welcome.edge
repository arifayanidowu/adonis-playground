@layout('layouts.app')

@section('title')
Home &mdash; Welcome to JobPostr
@endsection


@section('content')
  <div class="container">
  <h1 class="text-center mt-3">All Jobs {{ time() }}</h1>
    <hr class="hr">
    <ul class="list-group">
      @each(job in jobs)
      <li class="list-group-item">{{ job.title }}</li>
      <li class="list-group-item"><a href="{{ job.link }}" target="_blank">{{ job.link }}</a></li>
      <li class="list-group-item">{{ job.description }}</li>
      @endeach
    </ul>
  </div>
@endsection
